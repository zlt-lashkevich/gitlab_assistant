# Руководство пользователя GitLab Assistant

---

## Основные команды

### Информационные команды

- `/start` — Начать работу с ботом
- `/help` — Показать справку по всем командам
- `/status` — Показать ваш статус и активные подписки

### Управление токенами

- `/set_gitlab_token <токен>` — Установить GitLab токен
- `/set_github_token <токен>` — Установить GitHub токен
- 
Чтобы удалить токен, достаточнот отправить пустой токен:

```
/set_gitlab_token none
```

или

```
/set_github_token none
```

### Управление подписками

- `/subscribe` — Подписаться на проект (интерактивный режим)
- `/unsubscribe` — Отписаться от проекта (интерактивный режим)
- `/list_subscriptions` — Показать все активные подписки

---

## Создание подписки

### Пошаговая инструкция

1. **Запустите команду подписки:**

   ```
   /subscribe
   ```

2. **Выберите платформу:**

   Бот покажет кнопки:
   - 🐈 GitLab
   - 🐈‍⬛ GitHub

   Выберите нужную платформу

3. **Выберите проект:**

   Бот покажет список ваших проектов/репозиториев с пагинацией.
   
   Используйте кнопки:
   - **Назад** — предыдущая страница
   - **Вперед** — следующая страница
   - **Название проекта** — выбрать проект

4. **Выберите типы событий:**

   Бот покажет доступные типы событий. Вы можете выбрать несколько:

   **Для GitLab:**
   - Pipeline — статусы CI/CD пайплайнов
   - Merge Request — создание, обновление, слияние MR
   - Issue — создание, обновление, закрытие issues
   - Wiki — изменения в Wiki
   - Comment — комментарии к MR и issues

   **Для GitHub:**
   - Workflow — статусы GitHub Actions
   - Pull Request — создание, обновление, слияние PR
   - Issue — создание, обновление, закрытие issues
   - Comment — комментарии к PR и issues
   - Star — новые звезды репозитория

   Нажмите **Готово**, когда выберете все нужные события.

5. **Подтвердите подписку:**

   Бот покажет сводку вашей подписки:
   - Платформа
   - Проект
   - Выбранные события

   Нажмите:
   - **Подтвердить** — создать подписку
   - **Отменить** — отменить создание
   

   Бот создаст подписку и автоматически настроит webhook в вашем проекте.

---

##  Управление подписками

### Просмотр подписок

Отправьте команду:

```
/list_subscriptions
```

Бот покажет все ваши активные подписки с информацией:
- Платформа (GitLab/GitHub)
- Название проекта
- Типы событий

### Отписка от проекта

1. Отправьте команду:

   ```
   /unsubscribe
   ```

2. Выберите подписку, от которой хотите отписаться

3. Подтвердите действие

Бот удалит подписку и webhook из проекта.


ВАЖНО!
Каждый пользователь Telegram может использовать только один GitLab и один GitHub токен. Для работы с несколькими аккаунтами используйте разные Telegram аккаунты.